# 英语作文高亮标记工具使用说明

## 项目简介

这是一个专门为英语作文图片添加高亮标记的Python工具，支持四种不同的标记样式，并自动生成图例说明。

## 功能特点

### 四种高亮标记样式
1. **🔴 红色圆圈标记** - 用于标记重要单词或短语
2. **🔵 蓝色横线标记** - 用于标记句子或段落
3. **🟢 绿色双横线标记** - 用于标记重要句子
4. **🟡 黄色波浪线标记** - 用于标记需要特别注意的内容

### 主要功能
- 支持多种图片格式（**JPG/JPEG优先**、PNG、BMP、GIF、TIFF）
- 交互式图形界面，点击或拖拽添加标记
- 自动生成图例说明
- 实时预览标记效果
- 一键保存带图例的最终结果（支持JPG高质量保存）

## 安装和运行

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 运行主程序
```bash
python main.py
```

### 3. 创建示例图片（可选）
```bash
# 创建PNG格式示例
python create_sample.py

# 创建JPG格式示例（推荐）
python create_jpg_sample.py
```

## 使用步骤

### 1. 启动程序
运行 `python main.py` 启动图形界面

### 2. 加载图片
- 点击"选择图片"按钮
- 选择要标记的英语作文图片

### 3. 选择标记类型
- 在右侧选择要使用的标记类型：
  - 圆圈标记：点击单词添加圆圈
  - 横线标记：拖拽绘制横线
  - 双横线标记：拖拽绘制双横线
  - 波浪线标记：拖拽绘制波浪线

### 4. 添加标记
- **圆圈标记**：直接点击要标记的单词
- **线条标记**：按住鼠标左键拖拽绘制线条

### 5. 保存结果
- 点击"保存结果"按钮
- 选择保存位置和文件名
- 程序会自动添加图例并保存最终图片

### 6. 清除标记
- 点击"清除标记"按钮可以清除所有标记重新开始

## 文件说明

- `main.py` - 主程序，包含图形界面
- `highlight_tool.py` - 核心高亮标记功能
- `create_sample.py` - 创建示例图片的脚本
- `requirements.txt` - 项目依赖包
- `README.md` - 项目说明文档

## 技术特点

- 使用OpenCV进行图像处理
- 使用Tkinter创建图形界面
- 支持图片缩放和滚动
- 实时预览标记效果
- 自动生成专业图例

## 注意事项

1. 确保图片清晰度足够，便于准确标记
2. 标记时建议先选择标记类型再进行标记
3. 可以随时切换标记类型添加不同类型的标记
4. 保存前建议检查标记效果
5. 支持撤销功能（清除标记）

## 示例效果

运行 `python create_sample.py` 可以生成示例图片，展示四种标记样式的效果。

## 系统要求

- Python 3.7+
- Windows/Linux/macOS
- 至少2GB内存
- 支持常见图片格式
